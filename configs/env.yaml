# configs/env.yaml
# Environment Configuration for development and local testing.
# Project configuration and local testing variables.

env:
  NODE_ENV: development
  BINANCE_API_URL: https://api.binance.com/api/v3
  LOG_LEVEL: debug
  # Mock secret for pipeline validation
  TRADING_SECRET_MOCK: "sk_test_1234567890abcdef"
