# configs/env.yaml
# Environment Configuration for GitLab CI and local testing.
# THIS FILE IS EXCLUDED FROM GITHUB.

env:
  NODE_ENV: development
  BINANCE_API_URL: https://api.binance.com/api/v3
  LOG_LEVEL: debug
  # Mock secret for pipeline validation
  TRADING_SECRET_MOCK: "sk_test_1234567890abcdef"
